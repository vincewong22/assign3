#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct{
	char id[10];
	int score;
}grade;

int main(int argc, char* argv []){


/* grade *p;

p = malloc(100 * sizeof(grade));
if(p==0){
fprintf(stderr, "unable to allocate memory");
exit(1);
}

p[0].score = 95;
strcpy(p[0].id,"a00553100");


free(p); */
size_t nused, nalloc;
int *p;
int n;
p=0;
nused = nalloc = 0;


return 0;
}
